services:
  postgres:
    image: postgres:16-alpine
    environment:
      POSTGRES_DB: task_calendar
      POSTGRES_USER: task_calendar
      POSTGRES_PASSWORD: task_calendar
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
